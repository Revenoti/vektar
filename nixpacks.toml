# Nixpacks Configuration for Railway
# Forces npm instead of pnpm

providers = ["node"]

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npx serve -s dist -l $PORT'
